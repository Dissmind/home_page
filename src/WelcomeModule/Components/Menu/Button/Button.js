import React from "react";

// import './Button.css'

const Button = props => {

    return (
        <div style={{color : 'white'}}>
            {props.title}
        </div>
    )
}

export default Button
